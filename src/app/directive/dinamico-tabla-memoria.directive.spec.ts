import { DinamicoTablaMemoriaDirective } from './dinamico-tabla-memoria.directive';

describe('DinamicoTablaMemoriaDirective', () => {
  it('should create an instance', () => {
    const directive = new DinamicoTablaMemoriaDirective();
    expect(directive).toBeTruthy();
  });
});
